name: Build Wessam Pro Mod

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        # قمنا بتحديث هذه أيضاً إلى v4 لضمان الاستقرار
        uses: actions/checkout@v4 

      - name: Fetch ImGui Library
        run: |
          mkdir -p ImGui
          git clone --depth 1 https://github.com/ocornut/imgui.git temp_imgui
          cp temp_imgui/*.cpp ImGui/
          cp temp_imgui/*.h ImGui/
          cp temp_imgui/backends/imgui_impl_metal.* ImGui/
          cp temp_imgui/backends/imgui_impl_ios.* ImGui/
          rm -rf temp_imgui
          echo "✅ ImGui files are ready!"

      - name: Setup Theos
        run: |
          sudo xcode-select -s /Applications/Xcode.app
          export THEOS=~/theos
          git clone --recursive https://github.com/theos/theos.git $THEOS
          git clone https://github.com/Theos-SDKS/iOS-SDKs.git $THEOS/sdks
          echo "THEOS=$THEOS" >> $GITHUB_ENV

      - name: Build Dylib
        run: |
          export THEOS=~/theos
          make package FINALPACKAGE=1
          
      - name: Upload Build
        # هنا كان الخطأ؛ قمنا بالتحديث من v3 إلى v4
        uses: actions/upload-artifact@v4 
        with:
          name: Wessam-Pro-Build
          path: packages/*.deb
          
